# Python
__pycache__/
*.pyc
.venv/
venv/

# Secrets
.env

# Generated data (large, regenerable — don't commit)
data-pipeline/*.jsonl
!data-pipeline/seeds_example.txt

# Model artifacts (large binaries — don't commit to git)
model/*.task
model/*.tflite
model/checkpoints/

# Local-only third-party training media — never committed or distributed.
data-pipeline/raw-media/
*.mkv
*.mp4
*.srt
*.ass

# Internal design/planning docs — kept out of the public repo entirely,
# committed locally only on the docs-private branch (never pushed).
docs/

# Local backup bundles / scrub clones — never commit
_backup/
_scrub-tmp/

# OS
.DS_Store
Thumbs.db

# Movie mining outputs contain verbatim subtitle dialogue / film audio --
# local-only, never committed (see data-pipeline/movie_mining/README.md).
data-pipeline/movie_mining/out/
data-pipeline/seeds_subs_*.txt
# Seed files carrying verbatim film/subtitle dialogue (film_seeds.py output, merged batches from 6 on).
data-pipeline/seeds_*_film.txt
data-pipeline/seeds_*_subs.txt
data-pipeline/seeds_batch6.txt
data-pipeline/seeds_batch6b.txt
*.wav
